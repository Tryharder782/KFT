import { observer } from 'mobx-react-lite';
import React from 'react';

const birthDate = observer((birthDay) => {

	return (
		<div className='birthDate'>
			
		</div>
	);
});

export default birthDate;